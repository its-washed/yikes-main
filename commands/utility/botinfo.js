const { createEmbed, errorEmbed } = require('../../utils/embeds');
module.exports = { data: { name: 'botinfo', description: 'Bot info', usage: ',botinfo' }, aliases: ['bi'], cooldown: 5, async execute(message, client) { return message.reply({ embeds: [createEmbed({ color: 0x6c5ce7, title: client.user.tag, fields: [{ name: 'Servers', value: `${client.guilds.cache.size}`, inline: true }, { name: 'Users', value: `${client.users.cache.size}`, inline: true }, { name: 'Commands', value: `${client.commands.size}`, inline: true }, { name: 'Ping', value: `${client.ws.ping}ms`, inline: true }, { name: 'Uptime', value: `${Math.floor(client.uptime / 3600000)}h ${Math.floor((client.uptime % 3600000) / 60000)}m`, inline: true }, { name: 'Developer', value: '[its_washed](https://itswashed.lol)', inline: true }] })] }); } };

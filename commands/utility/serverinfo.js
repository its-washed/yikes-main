const { createEmbed, errorEmbed } = require('../../utils/embeds');
module.exports = { data: { name: 'serverinfo', description: 'Server info', usage: ',serverinfo' }, aliases: ['si'], cooldown: 5, async execute(message) { const g = message.guild; return message.reply({ embeds: [createEmbed({ color: 0x6c5ce7, title: g.name, fields: [{ name: 'ID', value: g.id, inline: true }, { name: 'Owner', value: `<@${g.ownerId}>`, inline: true }, { name: 'Members', value: `${g.memberCount}`, inline: true }, { name: 'Channels', value: `${g.channels.cache.size}`, inline: true }, { name: 'Roles', value: `${g.roles.cache.size}`, inline: true }, { name: 'Emojis', value: `${g.emojis.cache.size}`, inline: true }, { name: 'Boosts', value: `Level ${g.premiumTier} (${g.premiumSubscriptionCount || 0})`, inline: true }, { name: 'Created', value: `<t:${Math.floor(g.createdTimestamp / 1000)}:R>`, inline: true }] })] }); } };

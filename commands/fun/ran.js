const { createEmbed } = require('../../utils/embeds');
module.exports = { data: { name: 'ran', description: 'Anime run', usage: ',ran [@user]' }, aliases: ['running'], cooldown: 3, async execute(message) { const target = message.mentions.users.first() || message.author; return message.reply({ embeds: [createEmbed({ color: 0x6c5ce7, title: 'Running!', description: `${target} is running! 💨` })] }); } };

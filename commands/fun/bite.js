const { createEmbed, errorEmbed } = require('../../utils/embeds');
module.exports = { data: { name: 'bite', description: 'Bite someone', usage: ',bite [@user]' }, aliases: [], cooldown: 5, async execute(message) { const target = message.mentions.users.first(); if (!target) return message.reply({ embeds: [errorEmbed('Missing User', 'Usage: ,bite [@user]')] }); return message.reply({ embeds: [createEmbed({ color: 0xec4899, title: 'Bite!', description: `${message.author} bites ${target}! 🦷` })] }); } };

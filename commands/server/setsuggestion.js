const { createEmbed, errorEmbed } = require('../../utils/embeds');
const { hasPermission, isAdmin, isOwner } = require('../../utils/permissions');
module.exports = { data: { name: 'setsuggestion', description: 'Set suggestion channel', usage: ',setsuggestion [#channel]' }, aliases: ['suggestionchannel'], cooldown: 10, async execute(message, args) { if (!hasPermission(message.member, 'ManageGuild')) return message.reply({ embeds: [errorEmbed('No Permission', 'You need Manage Server.')] }); const channel = message.mentions.channels.first(); if (!channel) return message.reply({ embeds: [errorEmbed('Missing Channel', 'Usage: ,setsuggestion [#channel]')] }); return message.reply({ embeds: [createEmbed({ color: 0x22c55e, description: `Suggestion channel set to ${channel}.` })] }); } };

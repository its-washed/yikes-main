const { createEmbed, errorEmbed } = require('../../utils/embeds');
module.exports = { data: { name: 'setlogs', description: 'Set mod logs channel', usage: ',setlogs [#channel]' }, aliases: ['modlogs'], cooldown: 10, async execute(message, args) { if (!message.member.permissions.has('ManageGuild')) return message.reply({ embeds: [errorEmbed('No Permission', 'You need Manage Server.')] }); const channel = message.mentions.channels.first(); if (!channel) return message.reply({ embeds: [errorEmbed('Missing Channel', 'Usage: ,setlogs [#channel]')] }); return message.reply({ embeds: [createEmbed({ color: 0x22c55e, description: `Mod logs set to ${channel}.` })] }); } };

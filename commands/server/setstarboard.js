const { createEmbed, errorEmbed } = require('../../utils/embeds');
module.exports = { data: { name: 'setstarboard', description: 'Set starboard channel', usage: ',setstarboard [#channel] [threshold]' }, aliases: ['starboard'], cooldown: 10, async execute(message, args) { if (!message.member.permissions.has('ManageGuild')) return message.reply({ embeds: [errorEmbed('No Permission', 'You need Manage Server.')] }); const channel = message.mentions.channels.first(); if (!channel) return message.reply({ embeds: [errorEmbed('Missing Channel', 'Usage: ,setstarboard [#channel] [threshold]')] }); return message.reply({ embeds: [createEmbed({ color: 0xffd700, description: `Starboard set to ${channel}.` })] }); } };

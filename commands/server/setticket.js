const { createEmbed, errorEmbed } = require('../../utils/embeds');
module.exports = { data: { name: 'setticket', description: 'Set ticket category', usage: ',setticket [category]' }, aliases: ['ticketcategory'], cooldown: 10, async execute(message, args) { if (!message.member.permissions.has('ManageGuild')) return message.reply({ embeds: [errorEmbed('No Permission', 'You need Manage Server.')] }); const category = args[0]; if (!category) return message.reply({ embeds: [errorEmbed('Missing Category', 'Usage: ,setticket [category name]')] }); return message.reply({ embeds: [createEmbed({ color: 0x22c55e, description: `Ticket category set to **${category}**.` })] }); } };

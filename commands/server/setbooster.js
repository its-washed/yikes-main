const { createEmbed, errorEmbed } = require('../../utils/embeds');
const { hasPermission, isAdmin, isOwner } = require('../../utils/permissions');
module.exports = { data: { name: 'setbooster', description: 'Set booster role', usage: ',setbooster [@role]' }, aliases: ['boosterrole'], cooldown: 10, async execute(message, args) { if (!hasPermission(message.member, 'ManageGuild')) return message.reply({ embeds: [errorEmbed('No Permission', 'You need Manage Server.')] }); const role = message.mentions.roles.first(); if (!role) return message.reply({ embeds: [errorEmbed('Missing Role', 'Usage: ,setbooster [@role]')] }); return message.reply({ embeds: [createEmbed({ color: 0x22c55e, description: `Booster role set to ${role}.` })] }); } };

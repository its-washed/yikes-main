const { createEmbed, errorEmbed } = require('../../utils/embeds');
module.exports = { data: { name: 'setannouncement', description: 'Set announcement channel', usage: ',setannouncement [#channel]' }, aliases: ['announcementchannel'], cooldown: 10, async execute(message, args) { if (!message.member.permissions.has('ManageGuild')) return message.reply({ embeds: [errorEmbed('No Permission', 'You need Manage Server.')] }); const channel = message.mentions.channels.first(); if (!channel) return message.reply({ embeds: [errorEmbed('Missing Channel', 'Usage: ,setannouncement [#channel]')] }); return message.reply({ embeds: [createEmbed({ color: 0x22c55e, description: `Announcement channel set to ${channel}.` })] }); } };

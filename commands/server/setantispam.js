const { createEmbed, errorEmbed } = require('../../utils/embeds');
module.exports = { data: { name: 'setantispam', description: 'Configure anti-spam', usage: ',setantispam [on/off]' }, aliases: ['antispam'], cooldown: 10, async execute(message, args) { if (!message.member.permissions.has('ManageGuild')) return message.reply({ embeds: [errorEmbed('No Permission', 'You need Manage Server.')] }); const state = (args[0] || 'on').toLowerCase(); return message.reply({ embeds: [createEmbed({ color: 0x22c55e, description: `Anti-spam **${state === 'on' ? 'enabled' : 'disabled'}**.` })] }); } };

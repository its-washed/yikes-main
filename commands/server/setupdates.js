const { createEmbed, errorEmbed } = require('../../utils/embeds');
module.exports = { data: { name: 'setupdates', description: 'Set updates channel', usage: ',setupdates [#channel]' }, aliases: ['updateschannel'], cooldown: 10, async execute(message, args) { if (!message.member.permissions.has('ManageGuild')) return message.reply({ embeds: [errorEmbed('No Permission', 'You need Manage Server.')] }); const channel = message.mentions.channels.first(); if (!channel) return message.reply({ embeds: [errorEmbed('Missing Channel', 'Usage: ,setupdates [#channel]')] }); return message.reply({ embeds: [createEmbed({ color: 0x22c55e, description: `Updates channel set to ${channel}.` })] }); } };

const { createEmbed, errorEmbed } = require('../../utils/embeds');
module.exports = { data: { name: 'setautorole', description: 'Set auto role', usage: ',setautorole [@role]' }, aliases: ['autorole'], cooldown: 10, async execute(message, args) { if (!message.member.permissions.has('ManageGuild')) return message.reply({ embeds: [errorEmbed('No Permission', 'You need Manage Server.')] }); const role = message.mentions.roles.first(); if (!role) return message.reply({ embeds: [errorEmbed('Missing Role', 'Usage: ,setautorole [@role]')] }); return message.reply({ embeds: [createEmbed({ color: 0x22c55e, description: `Auto role set to ${role}.` })] }); } };
